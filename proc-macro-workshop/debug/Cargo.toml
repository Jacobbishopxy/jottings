[package]
name = "derive_debug"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1", features = ["extra-traits"] }
quote = "1"

[dev-dependencies]
trybuild = { version = "1", features = ["diff"] }
